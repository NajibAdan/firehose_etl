[project]
name = "atproto-firehose"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "confluent-kafka>=2.7.0",
    "websockets>=14.1",
]

[dependency-groups]
spark = [
    "py4j>=0.10.9.5",
    "pyspark>=3.3.3",
]
